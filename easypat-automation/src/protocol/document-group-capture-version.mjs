// v1 remains untouched as diagnostic evidence: its captured input was shell
// text, not SQL. New capture/evidence must never reuse the v1 fingerprint.
export const DOCUMENT_GROUP_CAPTURE=Object.freeze({
  schemaVersion:2,
  sourceSessionId:245,
  templateId:"matter-detail.document-group-intermediate.v2",
  requestMetadataFile:"document-group-intermediate-request.v2.json",
  responseEvidenceFile:"document-group-intermediate-response.v2.json",
});
