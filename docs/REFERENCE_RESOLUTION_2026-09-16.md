# 2026-09-16 연결 누락·모호 형식 검토

## 단계 요약

- 직전 감사의 연결 누락 15개 근거/고유 번호 5개와 모호 형식 8개 근거/고유 문자열 5개를 검토했다.
- Outlook을 새로 읽거나 메일을 이동·삭제·읽음 처리하지 않고, 운영 DB에 저장된 메일만 사용했다.
- `matter_linker` Terra/high가 현재 메시지·인용 이력·이메일 주소·외부대리인 참조번호를 분리했고, 실제 연결 후보는 `risk_verifier` Sol/high로 다시 검증했다.
- 신규 사건·별칭·축약 번호·업무 상태·입금 완료·Action은 생성하지 않았다.

## 반영 결과

- `T221357-PH(10)`의 입금내역 추가 알림 메일 1건을 기존 사건에 연결했다.
- 현재 제목과 본문이 전체 당소관리번호를 직접 명시한다는 점만 근거로 사용했다.
- 메일–사건 연결만 추가했으며 실제 입금 완료, 비용 처리 상태 또는 후속 Action은 확정하지 않았다.

| 항목 | 이전 | 이후 |
|---|---:|---:|
| 메일–사건 연결 | 258 | 259 |
| 번호 감사 전체 근거 | 69 | 68 |
| 연결 누락 근거 | 15 | 14 |
| 연결 누락 고유 번호 | 5 | 4 |

## 연결하지 않은 번호

- `P261574`, `P261574-PCT`, `P261574-PCT-EP`: 대상 메일의 현재 사건은 이미 연결된 `P261574` 또는 `P261574-EP`이고, 나머지 번호는 과거 견적·요청 인용 이력 또는 기초번호 절단에서 검출됐다.
- `P254480`: 사건번호가 아니라 인용된 수신 주소 `P254480.EXP.DOCS@...`의 이메일 로컬파트에서 검출됐다.
- 연결하지 않은 항목은 신규 사건이나 별칭으로 만들지 않았다.

## 모호 형식 판정

| 문자열 | 판정 |
|---|---|
| `JTX26281-MAD(TH)` | `Your Ref.`로 적힌 외부대리인 참조번호 |
| `Q33795` | `SVBB REF. Q33795/MRS/GL`의 외부대리인 참조번호 일부 |
| `P261806(263-16외)` | 여러 사건을 나타내는 표시용 범위이며 단일 전체 관리번호가 아님 |
| `PARK20090724` | `park20090724@gmail.com`의 이메일 로컬파트 |
| `LOVE110207` | `love110207@gmail.com`의 이메일 로컬파트 |

- 위 문자열은 당소관리번호 후보로 연결하지 않았다.
- 원시 감사는 증거 보존을 위해 계속 표시할 수 있다. 이번 모델 판정을 곧바로 전역 파서 규칙으로 자동 승격하지 않았으며, 규칙 변경은 회귀평가와 사용자 승인 후 별도로 진행한다.

## 검증과 다음 단계

- 사본 dry-run 후 운영 반영을 수행했고 기존 사건·업무·Action·회사·사람·그룹·Wiki는 변경되지 않았다.
- SQLite 무결성 정상, 외래키 오류 0이다.
- 다음 단계에서는 새로 연결된 입금내역 알림의 날짜별 사실 요약이 필요한지 검토하고, 보류 Action 2건의 최신 회신·실제 담당자 확인 또는 미등록 번호 27개의 잔여 분류로 진행한다.

## 후속 완료

- `T221357-PH(10)`의 새 입금 알림을 검증된 Wiki 사실로 추가하고 사건 Wiki `version=2`를 게시했다.
- 메일일 2026-09-11과 본문 입금일 2026-09-10을 구분했으며, 입금잔액·완납·업무 상태는 추정하지 않았다.
- 보류 Action 2건은 새 운영 Action으로 만들지 않고 `needs_review`로 유지했다. 상세 근거는 `PAYMENT_ACTION_FOLLOWUP_2026-09-16.md`에 기록했다.
