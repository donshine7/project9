# 2026-09-16 메일–사건 연결 보완

## 범위와 처리 원칙

- 저장된 메일 178건의 번호 감사에서 연결 누락 36항목/메일 23건을 대상으로 했다. Outlook 신규 수집·이동·읽음 변경은 하지 않았다.
- 실제 분석은 `matter_linker` Terra/high, 독립 검증은 `risk_verifier` Sol/high로 실행하고 입력 스냅샷·필드별 판단·실행 모델을 기록했다.
- 신규 사건, 그룹, 별칭, 회사·사람 관계, 업무 상태 또는 Action은 이번 단계의 변경 대상이 아니다.
- 사용자가 승인한 검증 통과 항목의 반영 범위 내에서 진행한다. 보류 판단을 사용자 직접 답변이나 확정값으로 기록하지 않는다.

## 검토 결과

- 연결 후보 22건을 생성했다. 제목의 완전한 관리번호 외에 브라질 연차료 대상 목록, 유럽출원 주소변경의 직접 회신 관계, 우선권 기초사건, 중국상표 등록증 요청 이력을 검토했다.
- 나머지 14항목은 과거 견적·명세서 인용 이력 또는 해외 대리인의 참조번호 여부를 구분해야 하므로 자동 연결하지 않는다. `P261574-EP`의 완료된 EasyPAT 등록을 과거 `P261574-PCT-EP` 표기와 병합하지 않는다.
- 미등록 번호 51증거 항목/31고유 번호, 미지원·모호 형식 9항목은 사건으로 자동 생성하지 않는다. 기존 사용자 확인 범위와 그룹 구성을 유지한다.
- 전송 검증에서 인용의 줄바꿈 누락 2건과 괄호 문구 누락 1건을 차단했다. 담당 에이전트가 원문과 일치하는 인용으로 교정한 뒤 후보를 저장했다. 국내 번호 3항목의 보류 이유도 전체 인용 이력을 재확인하여 교정했다. 규칙이나 프롬프트는 변경하지 않았다.

## 실행 안전장치

- `Prepare-LinkReview.ts`: 저장된 원문으로 범위가 고정된 감사·분석 패킷을 만든다. 새 결과 디렉터리만 허용한다.
- `Expand-LinkReview.ts`: 분석 결과를 계약 형식으로 기계적으로 변환하며 범위 밖 후보와 원문 불일치 인용을 거부한다.
- `Operate-LinkReview.ts`: 독립 검증을 모두 통과한 단일 `matterRef` 연결 후보만 수락한다. 운영 반영에는 동일 입력의 사본 검증 보고서가 필요하다.
- 사본 검증 이후 사건·업무 데이터, 기존 연결 또는 후보가 바뀌면 운영 반영을 중단한다. 기존 연결의 변경·삭제를 허용하지 않고 새 연결 수를 검사한다.
- 반영 전 백업, 반영 후 업무 테이블 불변성 및 SQLite 무결성·외래키 검사를 수행한다.
- 상세 원문·분석 결과와 검증 기록은 Git 제외된 `.analysis-private/link-review-20260916`에 보관한다.

## 반영 상태

- Sol/high 독립 검증 22/22 확인 후 DB 사본과 운영 DB에 각각 22건을 반영했다. 메일–사건 연결은 231→253건이다.
- 기존 메일, 사건, 업무, 담당 배정, Action, 메모, 회사·사람, 그룹·구성원, 사건–당사자 관계와 Wiki 개정 이력은 변경되지 않았다. 기존 메일 연결도 변경·삭제하지 않았다.
- SQLite 무결성 `ok`, 외래키 오류 0건이다. 운영 반영 전 백업: `sspat-work-2026-09-16T03-41-03-472Z.db`.
- 최신 번호 감사는 96→74항목이다. 연결 누락 검토 14, 미등록 번호 51증거 항목/31고유 번호, 미지원·모호 형식 9항목이다. 이는 74개 사건을 뜻하지 않는다.
- 대시보드 분석 API의 감사 목록을 갱신했다. 남은 항목은 사용자 확인 없이 자동 해소·반려 처리하지 않았다.
- `npm ci`, 린트, 1~4단계 테스트, Outlook 규칙 회귀 테스트, 빌드가 성공했다. 로컬 필수 12개 경로는 모두 HTTP 200으로 응답했다.
- 사건 214, 메일 178, 사건–당사자 관계 86, Action 9, Wiki 개정 이력 107건은 그대로다.

## 다음 검토 범위

후속 근거 검토와 D 디자인 분류 반영 결과는 [REFERENCE_REVIEW_2026-09-16.md](REFERENCE_REVIEW_2026-09-16.md)에 기록했다. 신규 사건 생성 없이 번호 상세에 독립 검증된 메일 요약을 표시한다.

- 미등록 번호는 완전한 사건번호 및 등록 여부를 확인한 후 별도 등록 절차로 다룬다. 제목/인용의 표기 차이만으로 별칭을 합치지 않는다.
- 과거 견적 번호와 해외 대리인 번호의 감사 항목은 실제 운영 사건과의 관계를 추가 확인한다. 이 단계의 보류를 학습용 정답이나 사용자의 거절로 계산하지 않는다.
- 그룹과 피지오 사건 범위는 앞선 사용자 확정을 유지하며 임의로 확대하지 않는다.
