# 2026-09-18 Outlook 신규 수신 자동분류 실동작 관찰

## 관찰 범위와 안전 조건

- 대상 저장소: `jtjang@sspat.net`
- 수신 기간: 2026-09-16 00:00부터 2026-09-19 00:00까지(KST)
- 최근 메일 261건을 제목·발신자·수신자·새 본문과 현재 폴더 기준으로 읽기 전용 확인했다.
- 메일을 열어 표시하거나 이동·삭제·재분류하지 않았고 읽음 상태도 변경하지 않았다.
- 수신시각이 없는 비정상 항목 9건은 비교에서 제외했다.

## 결과

현재 프로젝트의 `HiworksAutoClassifier.bas`와 동일한 결정 규칙으로 예상 폴더를 계산해 Outlook의 실제 폴더와 비교했다.

| 항목 | 건수 |
|---|---:|
| 관찰 메일 | 261 |
| 규칙상 폴더 이동 대상 | 241 |
| 예상 폴더와 실제 폴더 일치 | 228 |
| 받은편지함 유지가 정상인 미분류 | 20 |
| 받은편지함 분류 누락 | 4 |
| 다른 폴더에 위치 | 9 |

13건의 차이는 모두 같은 원인이 아니다. 규칙 적용 전 수신, 후속 수동 처리, 중복 메일 가능성이 섞여 있으므로 전부를 자동분류 실패로 세지 않았다. 다음 사례가 현재 실행본 상태를 판정하는 핵심 근거다.

- `Application_NewMailEx`는 동작 중이다. 2026-09-18 오후 수신된 `mslee@sspat.net` 상표 메일과 외부 송금 메일이 각각 `해외 상표`, `해외 견적/청구/정산`에 수신 직후 가까운 시점으로 이동했다.
- `@kpaa.or.kr` 최근 5건은 경조사·기타 폴더가 모두 예상과 일치했다.
- 마감리스트는 2026-09-17과 2026-09-18 신규 메일이 모두 `기일관리`로 이동했다. 2026-09-16 최초 1건만 받은편지함에 남아 있다.
- 2026-09-18 15:23 KST 수신된 `mslee@sspat.net` 메일 `RE: [업무확인요청] ... 씨유스킨 출원완료 보고 일괄 재발송 요청`은 최신 소스라면 최소 `해외 기타`로 이동해야 하지만 받은편지함에 남았다.
- `P261937-US` 명세서 초안 메일 2개는 최신 소스라면 `해외 특허`지만 실제로 `국내 특허`에 있다.

## 원인

최초 관찰 당시 Outlook이 사용하던 `VbaProject.OTM`의 SHA-256은 `b5bb9447577ee10f9d1cb2d80e9f866a7f870c610646b1f9c58a1f88b73c45dd`였다. 이는 2026-09-16 실행본과 같으며 다음 후속 문서에서 실행본 반영 보류로 기록한 상태와 일치한다.

- `OUTLOOK_MSLEE_FORCED_OVERSEAS_2026-09-17.md`
- `OUTLOOK_COUNTRY_SUFFIX_PATENT_2026-09-17.md`
- `OUTLOOK_OVERSEAS_MISSED_CALL_2026-09-16.md`

따라서 이벤트 연결이 끊긴 것이 아니라, 프로젝트 소스에만 있는 2026-09-17 이후 규칙이 실제 `HiworksRulesFinal`에 아직 동기화되지 않은 것이 핵심 원인이다.

## 판정과 다음 조치

- 4번 단계의 실동작 관찰은 완료했다.
- 최초 관찰 당시 상태는 `정상`이 아니라 `부분 적용·실행본 갱신 필요`였다.
- 아래 실행본 동기화로 원인을 해소했다. 이후 신규 수신 메일로 같은 읽기 전용 관찰을 반복한다. 기존 메일의 일괄 재분류는 별도 요청 없이는 실행하지 않는다.

## 2026-09-18 실행본 동기화 완료

- 변경 전 `VbaProject.OTM`을 `.analysis-private/outlook-runtime-sync-20260918/VbaProject-before-latest-sync.OTM`에 백업했다. 백업 SHA-256은 `b5bb9447577ee10f9d1cb2d80e9f866a7f870c610646b1f9c58a1f88b73c45dd`로 관찰 당시 실행본과 같다.
- Computer Use로 실제 `HiworksRulesFinal` 코드 전체를 프로젝트 원본으로 교체했다. `디버그 > Project1 컴파일` 후 컴파일 메뉴가 비활성화된 것을 확인하고 저장했다.
- `ThisOutlookSession.Application_NewMailEx` 호출 대상은 계속 `HiworksRulesFinal.ClassifyIncomingMail`이다. 비활성 모듈 `HiworksRules`와 기존 메일은 변경하지 않았다.
- 저장 후 OTM 크기는 409,600바이트, SHA-256은 `fe79197334459be653ce1341358e55f16c80c898f28b2a6be6455fa2e069a352`다.
- 저장된 OTM에서 `HiworksRulesFinal`, `mslee@sspat.net` 직접 발신 분기, `HasOverseasMatterReference`, `GetDirectOverseasDestination`, `Application_NewMailEx`를 다시 추출해 확인했다.
- Outlook을 완전히 종료한 뒤 재시작하고 해당 VBA 프로젝트의 매크로를 포함해 로드했다.
- 2026-09-18 17:42:14~17:47:06 KST 구간을 25개 대상 폴더에서 읽기 전용 재관찰했다. 신규 메일은 0건이고 오류도 0건이어서, 동기화 후 실제 신규 메일의 폴더 이동 검증은 다음 자연 수신 시점까지 대기한다.
