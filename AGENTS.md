# 프로젝트 운영 지침

## 범위

- 이 저장소는 상상특허 업무 자동화 프로그램의 유일한 소스 원본이다.
- 현재 프로젝트 루트는 `C:\ChatGPT\AI-Work\20_업무자동화\상상업무자동화`이다.
- 이전 경로인 `C:\Users\donsh\OneDrive\Documents\project9_workflow_automation`을 참조하거나 다시 만들지 않는다.

## 구성

- `HiworksAutoClassifier.bas`: Outlook (classic)용 Hiworks 메일 자동분류 모듈
- `ThisOutlookSession_Hiworks.txt`: Outlook 이벤트 연결 코드
- `dashboard`: 로컬 업무 허브, 사건 업무관리와 가출원 프로젝트 추적 대시보드
- `docs`: 구조와 설치·복구 문서
- `이메일_샘플`: 분류 규칙 검증용 원본 샘플

## 변경 규칙

- Outlook 분류 정책을 바꾸면 VBA, `dashboard/app/data.ts`, 정적 참고본, `docs/architecture.mmd`를 함께 갱신한다.
- 원본 이메일 샘플은 수정하지 않는다.
- 사용자 메일을 읽거나 이동하는 Outlook 작업은 사용자의 명시적 요청 없이는 실행하지 않는다.
- 비밀정보와 로컬 캐시는 Git에 추가하지 않는다.
- 대시보드는 외부 서비스가 아니라 `127.0.0.1:4173`에서 동작하는 로컬 도구로 유지한다.

## 사건 중심 업무관리 규칙

- 전체 목표 아키텍처와 업무 용어의 기준은 `docs/WORK_MANAGEMENT_ARCHITECTURE.md`이다.
- 사건은 사건명이 아니라 당소관리번호 전체 문자열로 식별한다.
- D로 시작하는 당소관리번호는 디자인 사건이다(2026-09-16 사용자 확정). 국가코드와 추가 접미사는 보존한다.
- T는 상표, PT는 상상특허 가출원, PPT는 상상플러스 가출원, P번호의 -S1/-S2는 시리즈의 개별 사건, -CN(PA)는 중국 가출원이다. 접미사를 삭제하거나 시리즈만으로 그룹을 확정하지 않는다.
- -CN은 정규 중국 출원으로 -CN(PA)와 구분한다. 중국 가출원은 보통 우선심사용이지만 개별 목적은 근거 없이 확정하지 않는다. 두 번호를 별칭으로 자동 병합하지 않는다.
- 일반출원, 우선심사출원, 메이킹, 기획, 가출원은 서비스 종류이며 업무종류로 취급하지 않는다.
- 업무종류, 업무단계, 현재상태를 서로 다른 필드로 유지한다.
- 사용자 확정값은 이메일 추정값으로 자동 덮어쓰지 않고, 모든 자동 판단에 근거와 신뢰도를 남긴다.
- 그룹 종류는 포트폴리오·시리즈·정부지원사업 등을 지원한다. 그룹 종류와 구성원·대표 사건은 별도로 확인하며 종류만으로 그룹을 자동 생성하지 않는다.
- 팀원 Action도 장진태 관리 화면에만 표시하며 상태 변경자는 장진태다.

## Codex 실행과 모델 라우팅

- 사용자가 메일 갱신·분석을 명시적으로 요청한 경우에만 Outlook 메일을 읽는다. 읽기 작업은 메일 이동·삭제·읽음 상태 변경을 하지 않는다.
- 메일 수집에서 대한변리사회 계열, 결재, 해외 출원 자동 안내, 과제 자동 안내 폴더는 제외하고 보낸 편지함은 포함한다.
- 결정적 파싱과 검증은 코드로 수행하고, 애매한 판단만 프로젝트 전용 에이전트에 위임한다.
- 메일 사실 구조화는 `mail_intake`, 사건 연결은 `matter_linker`, Action 판단은 `action_analyst`, 고위험 검증은 `risk_verifier`, Wiki 개정안은 `wiki_synthesizer`를 사용한다.
- 전문 에이전트는 후보와 근거만 반환한다. DB·상태·파일의 최종 변경은 주 작업이 검증한 뒤 수행한다.
- 작업별 모델과 추론 강도의 원본은 `config/llm-routing.toml`이며 역할 지침은 `config/agents/*.toml`이다. 주 작업은 하위 작업을 시작할 때 이 값을 명시한다.
- 사용자가 모델 변경을 요청하면 라우팅 원본과 해당 역할 설정, 아키텍처 문서를 함께 갱신한다.
- 실제 실행 시 동작, 에이전트, 모델, 추론 강도, 프롬프트 버전, 입력 스냅샷과 결과를 `decision_run` 이력에 기록한다.
- 모델·규칙 판단은 사용자에게 표시하거나 반영하기 전에 필드별 `decision_item`으로 기록하고 입력 스냅샷을 연결한다.
- 사용자 수락·수정·거절·누락 추가는 기존 판단을 덮어쓰지 않고 `user_feedback`과 확정 `event`로 기록한다.
- 새 증거, 일회성 예외, 표현 선호와 실제 판단 오류를 구분한다. 검토하지 않은 판단은 성공 사례로 계산하지 않는다.
- 사용자 수정으로 규칙·프롬프트·모델 라우팅을 자동 변경하지 않는다. 개선안은 `docs/DECISION_FEEDBACK_DESIGN.md`의 회귀 평가와 사용자 승인을 통과해야 한다.

## 완료 조건

- `dashboard`에서 `npm ci`, `npm run lint`, `npm run test:phase1`, `npm run test:phase2`, `npm run test:phase3`, `npm run test:phase4`, `npm run build`가 성공해야 한다.
- `/`, `/mail`, `/matters`, `/analysis`, `/wiki`, `/provisional`, `/api/projects`, `/api/matters`, `/api/work-db/status`, `/api/mail-sync`, `/api/analysis`, `/api/wiki`가 로컬 서버에서 오류 없이 응답해야 한다.
- 새 프로젝트 초기화는 먼저 dry-run으로 검증하고 기존 폴더를 덮어쓰지 않아야 한다.
